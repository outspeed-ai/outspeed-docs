---
title: "Widget Example"
sidebarTitle: "Widget Example"
description: "Seamlessly incorporate the realtime voice/video into your website to boost visitor engagement."
---

Elevate your website's user engagement with the realtime Voice Widget. This powerful feature allows visitors to interact with a voice assistant, providing a modern and interactive way to access your services.

## Installation Guide

<Steps>
  <Step title="Insert the Widget Snippet">
    Paste the following snippet into your website’s HTML, ideally just before the closing `</body>` tag.

    ```html
    <script>
    </script>
    ```

  </Step>

  <Step title="Events">
    Here are some examples of events you can listen for and actions you can control.
    ```js
      realtime.on('speech-start', () => {
        console.log('Speech has started');
      });

      realtime.on('speech-end', () => {
        console.log('Speech has ended');
      });

      realtime.on('call-start', () => {
        console.log('Call has started');
      });

      realtime.on('call-end', () => {
        console.log('Call has stopped');
      });

      realtime.on('volume-level', (volume) => {
        console.log(`Assistant volume level: ${volume}`);
      });

      realtime.on('message', (message) => {
        console.log(message);
      });

      realtime.on('error', (e) => {
        console.error(e)
      });
    ```

  </Step>
</Steps>

