<Warning>
  To use the latest version of the outspeed module, run the following command: 
  ```bash 
  pip install --upgrade "outspeed[silero]>=0.1.151" 
  ```
</Warning>

<CodeGroup>
```bash MacOS/Linux
# Install the outspeed package
pip install --no-cache-dir "outspeed[silero]"
```

```batch Windows
REM Install the outspeed package
pip install --no-cache-dir "outspeed[silero]"
```

</CodeGroup>
